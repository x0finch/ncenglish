{
  "name": "nce-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @nce/app dev",
    "build": "pnpm --filter @nce/app build",
    "e2e": "pnpm --filter @nce/app e2e",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test:run",
    "test:run": "pnpm -r test:run"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "typescript": "^5.7.2",
    "vite": "^8.0.1",
    "vitest": "^4.1.0"
  }
}
